# library-reads

Read a Libby export and optional manual entries into a typed array of recent reads, with Open Library enrichment.

## Status

This package is in active development and pre-1.0. The API may change between releases without notice. Pin a version if you depend on it.

## Why

TODO: explain the motivation here (the books equivalent of a "Lately" feed, sourced from a Libby export instead of an RSS feed).

## Usage

TODO: document installation and the `getReads` API once it lands.

## License

MIT, Anthony Liddle.
